
sequenceDiagram
participant browser
participant server

Note right of browser: User writes note and clicks Save

browser->>server: POST /exampleapp/new_note_spa (note data)
activate server
server-->>browser: JSON response (success)
deactivate server

Note right of browser: Browser updates UI with new note (no page reload)
